import { stringSafe } from 'bustle/helpers/string-safe';
import { module, test } from 'qunit';

module('Unit | Helper | string safe');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = stringSafe([42]);
  assert.ok(result);
});
